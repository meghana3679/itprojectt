import { Button } from "@mui/material";
export const MuiButton =() => {
      return (
        <div>
        <Button variant="text">proxy</Button>
        <Button variant="contained">interview</Button>
        <Button variant="outlined">support</Button>
        
        </div>
      );
};